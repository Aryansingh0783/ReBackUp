# Normalise to LF in the repo. rustfmt.toml sets newline_style = "Unix", so
# committing CRLF makes cargo fmt --check fail on Windows runners only.
* text=auto eol=lf
*.png binary
*.ico binary
*.icns binary
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.tmpl text eol=crlf